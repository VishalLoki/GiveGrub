<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/primary_the_darkest_color"/> <!-- Adjust border width and color -->
    <corners android:radius="22.5dp" /> <!-- Make the corners rounded -->
</shape>